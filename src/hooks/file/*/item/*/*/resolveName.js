
/**
 * @group hook
 * @function
 * This is a flag-specific hook. Some flags like <code>extends</code> 
 * or <code>implements</code> have it.
 * @param {Item} item Item to which the flag is added
 * @param {string} flagName
 * @param {string} flagContent
 * @returns {string} Full name of reference item
 */