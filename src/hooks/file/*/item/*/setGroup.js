
var globalCache = require("../../../../../var/globalCache.js");

/**
 * @group hook
 * @function
 * Event hook that is called when item is assigned to a new group.
 * @param {Item} item
 */
module.exports = globalCache.add("file.*.item.*.setGroup", 
    function(item) {

        
    });